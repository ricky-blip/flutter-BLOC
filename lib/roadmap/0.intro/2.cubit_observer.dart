import 'package:flutter/material.dart';

class CubitObserver extends StatelessWidget {
  const CubitObserver({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
